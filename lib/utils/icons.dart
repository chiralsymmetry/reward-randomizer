import 'package:flutter/material.dart';

class AppIcons {
  static const aboutWebsite = Icon(Icons.home);
  static const activitiesDropdownSort = Icon(Icons.sort);
  static const activityAdd = Icon(Icons.add);
  static const activityButtonWaiting = Icon(Icons.hourglass_top);
  static const activityDelete = Icon(Icons.delete);
  static const activityIconDefault = Icon(Icons.abc);
  static const activityOrderLabelAscending = Icon(Icons.label);
  static const activityOrderLabelDescending = Icon(Icons.label);
  static const activityOrderNewestFirst = Icon(Icons.schedule);
  static const activityOrderOldestFirst = Icon(Icons.schedule);
  static const activityOrderQuestionAscending = Icon(Icons.sort_by_alpha);
  static const activityOrderQuestionDescending = Icon(Icons.sort_by_alpha);
  static const activityOrderRandom = Icon(Icons.shuffle);
  static const activityOrderUserDefined = Icon(Icons.swipe_vertical);
  static const activitySave = Icon(Icons.check);
  static const darkModeDark = Icon(Icons.dark_mode);
  static const darkModeLight = Icon(Icons.light_mode);
  static const darkModeSystem = Icon(Icons.settings_suggest);
  static const languageSystem = Icon(Icons.settings_suggest);
  static const pageAbout = Icon(Icons.info);
  static const pageActivities = Icon(Icons.published_with_changes);
  static const rewardAdd = Icon(Icons.add);
  static const rewardDelete = Icon(Icons.delete);
  static const settingDarkMode = Icon(Icons.dark_mode);
  static const settingDelay = Icon(Icons.hourglass_top);
  static const settingLanguage = Icon(Icons.language);
  static const settingsPage = Icon(Icons.settings);
  static const settingsSave = Icon(Icons.check);
}
